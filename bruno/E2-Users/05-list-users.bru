meta {
  name: 05 - List Users
  type: http
  seq: 5
}

get {
  url: {{base_url}}/api/users/?page=2
  body: none
  auth: bearer
}

params:query {
  page: 2
}

auth:bearer {
  token: {{token}}
}

tests {
  test("List users - 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
