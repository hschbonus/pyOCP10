meta {
  name: 05 - List Users
  type: http
  seq: 5
}

get {
  url: {{base_url}}/api/users/?page=1
  body: none
  auth: bearer
}

params:query {
  page: 1
}

auth:bearer {
  token: {{token}}
}

tests {
  test("List users - 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
