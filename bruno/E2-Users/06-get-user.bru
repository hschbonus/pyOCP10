meta {
  name: 06 - Get User
  type: http
  seq: 6
}

get {
  url: {{base_url}}/api/users/{{user_id}}/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("Get user - 200", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.have.property("username");
  });
}
