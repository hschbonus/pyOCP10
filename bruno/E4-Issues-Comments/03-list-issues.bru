meta {
  name: 03 - List Issues
  type: http
  seq: 3
}

get {
  url: {{base_url}}/api/issues/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("List issues - 200", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.have.property("results").that.is.an("array");
    expect(res.getBody()).to.have.property("count");
  });
}
