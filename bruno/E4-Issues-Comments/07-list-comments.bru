meta {
  name: 07 - List Comments
  type: http
  seq: 7
}

get {
  url: {{base_url}}/api/comments/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("List comments - 200", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.be.an("array");
  });
}
