meta {
  name: 04 - List Comments sans token
  type: http
  seq: 4
}

get {
  url: {{base_url}}/api/comments/
  body: none
  auth: none
}

tests {
  test("Sans token - 401", function() {
    expect(res.getStatus()).to.equal(401);
  });
}
