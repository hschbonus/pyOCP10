meta {
  name: 02 - Get Project (Charlie - non contributeur)
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/projects/{{project_id}}/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{charlie_token}}
}

tests {
  test("Non contributeur - retrieve - 403", function() {
    expect(res.getStatus()).to.equal(403);
  });
}
