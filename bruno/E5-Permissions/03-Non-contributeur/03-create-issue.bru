meta {
  name: 03 - Create Issue (Charlie - non contributeur)
  type: http
  seq: 3
}

post {
  url: {{base_url}}/api/issues/
  body: json
  auth: bearer
}

auth:bearer {
  token: {{charlie_token}}
}

body:json {
  {
    "name": "Issue intrus",
    "description": "Charlie essaie de créer une issue",
    "tag": "bug",
    "priority": "low",
    "status": "to do",
    "project": {{project_id}}
  }
}

tests {
  test("Non contributeur - create issue - 403", function() {
    expect(res.getStatus()).to.equal(403);
  });
}
