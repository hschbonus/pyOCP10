meta {
  name: 04 - Create Comment (Charlie - non contributeur)
  type: http
  seq: 4
}

post {
  url: {{base_url}}/api/comments/
  body: json
  auth: bearer
}

auth:bearer {
  token: {{charlie_token}}
}

body:json {
  {
    "description": "Charlie essaie de commenter",
    "issue": {{issue_id}}
  }
}

tests {
  test("Non contributeur - create comment - 403", function() {
    expect(res.getStatus()).to.equal(403);
  });
}
