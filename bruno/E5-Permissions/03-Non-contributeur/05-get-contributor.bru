meta {
  name: 05 - Get Contributor (Charlie - non contributeur)
  type: http
  seq: 5
}

get {
  url: {{base_url}}/api/contributors/{{bob_contributor_id}}/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{charlie_token}}
}

tests {
  test("Non contributeur - get contributor - 403", function() {
    expect(res.getStatus()).to.equal(403);
  });
}
