meta {
  name: 02 - Get Project (Bob - contributeur)
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/projects/{{project_id}}/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bob_token}}
}

tests {
  test("Contributeur - retrieve - 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
