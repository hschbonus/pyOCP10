meta {
  name: 04 - Delete Project (Bob - non auteur)
  type: http
  seq: 4
}

delete {
  url: {{base_url}}/api/projects/{{project_id}}/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bob_token}}
}

tests {
  test("Contributeur non auteur - delete - 403", function() {
    expect(res.getStatus()).to.equal(403);
  });
}
