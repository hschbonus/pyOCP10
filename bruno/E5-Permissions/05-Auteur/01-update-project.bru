meta {
  name: 01 - Update Project (Alice - auteur)
  type: http
  seq: 1
}

patch {
  url: {{base_url}}/api/projects/{{project_id}}/
  body: json
  auth: bearer
}

auth:bearer {
  token: {{alice_token}}
}

body:json {
  {
    "name": "Projet E5 modifié"
  }
}

tests {
  test("Auteur - update project - 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
